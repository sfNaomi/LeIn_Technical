<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>NextDeliveryDate__c</fullName>
    <externalId>false</externalId>
    <formula>IF(WEEKDAY(TODAY()) = (VALUE(LEFT( PrimaryGridNumber__c ,1))+ 1),
TODAY(),
IF (WEEKDAY(TODAY()) = 7,
TODAY()+ VALUE(LEFT( PrimaryGridNumber__c ,1))+1,
IF (WEEKDAY(TODAY()) = 1,
TODAY()+ VALUE(LEFT( PrimaryGridNumber__c ,1)),
IF(WEEKDAY(TODAY()) &lt; (VALUE(LEFT( PrimaryGridNumber__c ,1))+ 1),
TODAY()+ (VALUE(LEFT( PrimaryGridNumber__c ,1))+1 - WEEKDAY(TODAY())),
IF(WEEKDAY(TODAY()) &gt; (VALUE(LEFT( PrimaryGridNumber__c ,1))+ 1),
TODAY()+ (VALUE(LEFT( SecondaryGridNumber__c ,1))+1 - WEEKDAY(TODAY())),
TODAY()))))
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Next Delivery Date</label>
    <required>false</required>
    <type>Date</type>
</CustomField>
